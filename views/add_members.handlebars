<br>
{{#if errors}}
    {{#each errors}}
        <div class="alert alert-warning">
        <strong>Warning!</strong> {{msg}}.
        </div>
    {{/each}}
{{/if}}

{{#if success_msg}}
        <div class="alert alert-warning">
        <strong>Warning!</strong> {{success_msg}}.
        </div>
{{/if}}

<br>
<h4>Add new members</h4>
<form action="/cms/dashboard/members/new_add" method="POST">
  <div class="form-group">
    <label for="name">Name:</label>
    <input type="text" class="form-control" name="name">
  </div>
    <div class="form-group">
    <label for="email">Email:</label>
    <input type="email" class="form-control" name="email">
  </div>
  <div class="form-group">
    <label for="sel1">Role:</label>
    <select class="custom-select" name="role">
      {{#each roles}}
        <option value={{id}}>{{title}}</option>
      {{/each}}
    </select>
  </div>
  <div class="form-group">
    <label for="name">Password:</label>
    <input type="password" class="form-control" name="password" id="password">
    <i class="far fa-eye" onclick="showPassword();" ></i>
  </div>
  <button type="submit" class="btn btn-primary">Submit</button>
  <a href="/cms/dashboard/1"><button type="button" class="btn btn-light">Back</button></a>
</form>

<script>
      function showPassword() {
        var x = document.getElementById("password");
        if (x.type === "password") {
            x.type = "text";
        } else {
            x.type = "password";
        }
    }
</script>